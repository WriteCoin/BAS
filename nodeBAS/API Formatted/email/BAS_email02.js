function BAS_imap_client_set_proxy(proxy, proxy_type, login, password) { const args = _arguments(); const proxy = args.proxy || ""; const proxy_type = args.proxy_type || "http"; const login = args.login || ""; const password = args.password || ""; ; const hash = proxy_set_hash(proxy, proxy_type, login, password);; imap_client_set_proxy(hash.server, hash.Port, hash.IsHttp, hash.name, hash.password);};