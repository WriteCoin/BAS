function BAS_coords_drag_finish() { const args = _arguments(); const x = args.x; const y = args.y; const mouseSettings = args.mouse; const mouseSettingsObj = mouseSettings ? { speed: mouseSettings.speed || 100, gravity: mouseSettings.gravity || 6, deviation: mouseSettings.deviation || 2.5; } : {};; const move_settings = mouseSettingsObj; move_seetings["do_mouse_up"] = "true"; move(x, y, move_settings)!;};