function BAS_mouse_move_and_click() { const args = _arguments(); const x = args.x; const y = args.y; const isCtrlClick = args.isCtrlClick || false; const typeClick = args.typeClick || "<MOUSELEFT>"; const mouseSettings = args.mouse; const wait_full_load = args.wait_full_load || false; const timeout = args.timeout ;; if (mouseSettings) { move(x, y, { speed: mouseSettings.speed || 100, gravity: mouseSettings.gravity || 6, deviation: mouseSettings.deviation || 2.5; })!; } else { move(x, y)!; }; if (!isCtrlClick && typeClick === "<MOUSELEFT>") { mouse(x, y)!; } else { const key = isCtrlClick ? "<CONTROL><MOUSELEFT>" : typeClick; _type(key, 100)!; }; if (wait_full_load) { if (timeout) { waiter_timeout_next(timeout); }; wait_async_load()!; };};