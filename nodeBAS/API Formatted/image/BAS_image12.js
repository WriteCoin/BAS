function BAS_image_fill( image_id, x, y, width, height, red, green, blue, alpha;) { x = x || 0; y = y || 0; width = width || 100; height = height || 100; red = red || 255; green = green || 255; blue = blue || 255; alpha = alpha || 255; native( "imageprocessing", "fill", image_id +; "," +; x +; "," +; y +; "," +; width +; "," +; height +; "," +; red +; "," +; green +; "," +; blue +; "," +; alpha; );};