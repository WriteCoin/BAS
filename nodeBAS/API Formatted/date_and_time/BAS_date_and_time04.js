function BAS_change_date( value, year, month, day, hour, minutes, seconds, milliseconds;) { const date = _parse_date(value, "auto"); const offset = date.getTimezoneOffset(); const newDate = new Date(date.getTime()); newDate.getTimezoneOffset = function () { return offset; }; if (year !== -1) newDate.setFullYear(year); if (month !== -1) newDate.setMonth(month - 1); if (day !== -1) newDate.setDate(day); if (hour !== -1) newDate.setHours(hour); if (minutes !== -1) newDate.setMinutes(minutes); if (seconds !== -1) newDate.setSeconds(seconds); if (milliseconds !== -1) newDate.setMilliseconds(milliseconds); return newDate;};